var controller = require('../src/js/controllers/home.js');

describe('Home Controller Spec', function() {

  it('controller should be object', function() {
    expect(controller).toEqual(jasmine.any(Object));
  });

});
